const name = 'Saifcasm';

const repoCount = 50;

console.log(name + " has the repo count: "+repoCount);

console.log(`Name here is ${name} and the repo count is ${repoCount}`);

const userName = new String('Saifcasm');
console.log(userName);

console.log(userName.__proto__);

console.log(userName.charAt(5));

console.log(userName.indexOf("f"));

console.log(userName.toUpperCase());